# FastAPI
PORT=8080
ENVIRONMENT=dev
CORS_ORIGINS=http://localhost:3000

# Elastic
ELASTIC_URL=http://localhost:9200
ELASTIC_API_KEY=
ELASTIC_INDEX_DOCTORS=doctors
ELASTIC_INDEX_EVIDENCE=evidence

# BigQuery / Costs (placeholder)
BQ_PROJECT=
BQ_DATASET=curated
COST_TABLE=cost_table


# ============================================
# AI Chat Service - Vertex AI Configuration
# ============================================
# Reference: https://cloud.google.com/vertex-ai/generative-ai/docs/start/quickstart

# --------------------------------------------
# Google Cloud Vertex AI Configuration
# --------------------------------------------
# Enable Vertex AI mode (required)
GOOGLE_GENAI_USE_VERTEXAI=True

# Your Google Cloud Project ID (required)
# Example: GOOGLE_CLOUD_PROJECT=YOUR_PROJECT
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud region/location (required)
# Common values: us-central1, us-east1, europe-west1, asia-northeast1
GOOGLE_CLOUD_LOCATION=us-central1

# --------------------------------------------
# Model Configuration (optional)
# --------------------------------------------
# Default model to use
# Available models:
# - gemini-2.5-flash (recommended, fast and efficient)
# - gemini-2.5-pro (most capable)
# - gemini-2.0-flash-001 (previous version)
GENAI_MODEL=gemini-2.5-flash

# Generation parameters (optional)
GENAI_TEMPERATURE=0.7
GENAI_TOP_P=0.95
GENAI_TOP_K=40
GENAI_MAX_OUTPUT_TOKENS=8192
GENAI_SAFETY_THRESHOLD=BLOCK_MEDIUM_AND_ABOVE

# --------------------------------------------
# Speech-to-Text Configuration (optional)
# --------------------------------------------
# Reference: https://cloud.google.com/speech-to-text/docs/transcribe-streaming-audio

# Audio sample rate in Hz (default: 16000)
SPEECH_SAMPLE_RATE=16000

# Language code for recognition
# Common values: en-US, zh-CN, zh-TW, ja-JP, es-ES, fr-FR, de-DE
SPEECH_LANGUAGE_CODE=en-US

# Audio encoding format (default: LINEAR16)
SPEECH_ENCODING=LINEAR16

# Speech recognition model (default: default)
# Available models: default, command_and_search, phone_call, video
SPEECH_MODEL=default

# Enable automatic punctuation (default: True)
SPEECH_ENABLE_AUTOMATIC_PUNCTUATION=True

# Single utterance mode - stop after detecting end of speech (default: False)
SPEECH_SINGLE_UTTERANCE=False

# --------------------------------------------
# Authentication
# --------------------------------------------
# This service uses Application Default Credentials (ADC)
# 
# For local development:
#   gcloud auth application-default login
#   gcloud config set project YOUR_PROJECT_ID
#
# For production (service account):
#   Set GOOGLE_APPLICATION_CREDENTIALS to the path of your service account key JSON file
#
# Make sure your account/service account has the following IAM roles:
#   - Vertex AI User
#   - AI Platform User

# --------------------------------------------
# How to Set These Variables
# --------------------------------------------
# Windows (PowerShell):
#   $env:GOOGLE_CLOUD_PROJECT="your-project-id"
#   $env:GOOGLE_CLOUD_LOCATION="us-central1"
#   $env:GOOGLE_GENAI_USE_VERTEXAI="True"
#
# Windows (CMD):
#   set GOOGLE_CLOUD_PROJECT=your-project-id
#   set GOOGLE_CLOUD_LOCATION=us-central1
#   set GOOGLE_GENAI_USE_VERTEXAI=True
#
# Linux/Mac:
#   export GOOGLE_CLOUD_PROJECT=your-project-id
#   export GOOGLE_CLOUD_LOCATION=us-central1
#   export GOOGLE_GENAI_USE_VERTEXAI=True
#
# Or use a .env file with python-dotenv (automatic loading)